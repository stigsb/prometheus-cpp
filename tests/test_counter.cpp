#include <gtest/gtest.h>
// #include <prometheus/counter.hpp>

// Placeholder — implementation pending.
// Tests will verify:
//   Counter::inc() increments by 1
//   Counter::inc(n) increments by n
//   Counter::load() returns current value
//   Negative delta is rejected (debug assert)

TEST(CounterTest, Placeholder) {
    SUCCEED();
}
