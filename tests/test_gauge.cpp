#include <gtest/gtest.h>
// #include <prometheus/gauge.hpp>

// Placeholder — implementation pending.
// Tests will verify:
//   Gauge::set(v), inc(), dec()
//   Gauge::load() returns current value
//   Gauge can go negative (unlike Counter)

TEST(GaugeTest, Placeholder) {
    SUCCEED();
}
